# when you first pull this, use this command in terminal:
# `chmod +x start.sh`
# this will allow you to run the whole app (back and front end) with the command
# `./start.sh`

gradlew bootRun & cd react-event-app && npm run start