var debug;
// Export a function as the module
module.exports = function () {
  // If 'debug' is not defined, try to require the 'debug' module
  if (!debug) {
    try {
      /* eslint global-require: off */
      debug = require("debug")("follow-redirects");
    }
    catch (error) {  /* If an error occurs during require, do nothing */  }
    // If 'debug' is not a function, assign an empty function to it
    if (typeof debug !== "function") {
      debug = function () { /* */ };
    }
  }
  // Call the 'debug' function with the provided arguments
  debug.apply(null, arguments);
};
